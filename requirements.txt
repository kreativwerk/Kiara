fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
jinja2==3.1.4
python-multipart==0.0.9
cryptography==42.0.8
pydantic==2.7.4
pydantic-settings==2.3.4
pdfplumber==0.11.1
pytest==8.2.2
httpx==0.27.0

# Texterkennung (OCR) für Scans/Fotos. Braucht zusätzlich das Systempaket
# tesseract-ocr (+ tesseract-ocr-deu); ohne läuft Kiara trotzdem, nur ohne OCR.
pytesseract==0.3.10
pypdfium2==4.30.0
pillow-heif==0.16.0

# Optional: Google-Drive-Spiegelung (Feature funktioniert ohne diese Pakete nicht,
# der Rest von Kiara läuft aber auch ohne sie).
google-api-python-client==2.134.0
google-auth-oauthlib==1.2.0
google-auth==2.30.0
